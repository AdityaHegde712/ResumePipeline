# API Test Results

**Generated:** 2026-06-29T17:33:01+00:00
**Base URL:** http://localhost:8000
**Discovery source:** manual routes file (`.agent-tasks/batch2_routes.json`)
**Endpoints tested:** 5
**Passed:** 3 | **Failed:** 2 | **Skipped:** 0 | **Flaky (recovered):** 0

---

## Summary

| Method | Path | Status | Result | Notes |
|--------|------|--------|--------|-------|
| GET | /api/projects | 200 | PASS | - |
| GET | /api/projects/search | 200 | PASS | Redirect followed: `?q=test` added automatically |
| GET | /api/projects/{project_id} | 404 | FAIL | Path param `project_id` substituted as `test` — not a valid project ID |
| POST | /api/projects/refresh | 200 | PASS | Payload: `{}` |
| POST | /api/projects/match | 500 | FAIL | Payload auto-generated; server error on both attempts |

---

## Endpoint Details

### FAIL: GET /api/projects/{project_id} — Attempt Log

**Attempt 1**
- Sent: `GET http://localhost:8000/api/projects/test`
- Response status: `404 Not Found`
- Response body:
  ```json
  {"detail": "Project 'test' not found."}
  ```
- Path param substituted: `project_id` = `"test"` (type: str → default value)

**Attempt 2 (retry after 1.5s)**
- Same URL
- Response status: `404 Not Found`
- Response body:
  ```json
  {"detail": "Project 'test' not found."}
  ```
- **Result: FAILED**

---

### FAIL: POST /api/projects/match — Attempt Log

**Attempt 1**
- Sent: `POST http://localhost:8000/api/projects/match`
- Payload:
  ```json
  {
    "job_title": "test",
    "company_name": "test",
    "job_description": "test"
  }
  ```
- Response status: `500 Internal Server Error`
- Response body:
  ```json
  {"detail": "Project matching failed."}
  ```
- Error type: Server error (5xx)

**Attempt 2 (retry after 1.5s)**
- Same payload
- Response status: `500 Internal Server Error`
- Response body:
  ```json
  {"detail": "Project matching failed."}
  ```
- **Result: FAILED**

---

## Debugger Handoff

> This section is structured for a debugger agent to parse and act on.
> Each issue includes a hypothesis and recommended entry point.

### Issue #1: GET /api/projects/{project_id} — 404 for unknown project ID

- **Endpoint:** `GET /api/projects/{project_id}`
- **Status received:** `404`
- **Expected:** `200` with project data
- **Payload sent:** none (GET request)
- **Path param:** `project_id` = `"test"`
- **Response excerpt:**
  ```json
  {"detail": "Project 'test' not found."}
  ```
- **Hypothesis:** The path parameter substitution used a generic string `"test"` which is not a valid project ID; the endpoint works correctly but needs a real project ID from the list returned by `GET /api/projects`.
- **Recommended entry point:** `backend/app/api/projects.py:74` — `get_project()` handler; the 404 is expected behavior for invalid IDs. For automated testing, the runner should first call `GET /api/projects` and use a real project ID.
- **Priority:** Low

### Issue #2: POST /api/projects/match — 500 Internal Server Error

- **Endpoint:** `POST /api/projects/match`
- **Status received:** `500`
- **Expected:** `200` with matched project results
- **Payload sent:**
  ```json
  {"job_title": "test", "company_name": "test", "job_description": "test"}
  ```
- **Response excerpt:**
  ```json
  {"detail": "Project matching failed."}
  ```
- **Hypothesis:** The matching logic is raising an unhandled exception — likely a null reference, missing database column, or failure in the embedding/pipeline step when processing the generic payload values.
- **Recommended entry point:** `backend/app/api/projects.py:106` — `match_projects()` handler; inspect the full stack trace and check the matching service's error handling around embedding generation or DB lookup.
- **Priority:** High

---

**Recommended next steps for debugger agent:**

1. Inspect `backend/app/api/projects.py` around line 106 — `match_projects()` handler and catch/log the full exception
2. Check the matching service implementation for null-safety on the generated embedding
3. Run `curl -X POST http://localhost:8000/api/projects/match -H "Content-Type: application/json" -d '{"job_title":"test","company_name":"test","job_description":"test"}'` and check server logs for the traceback

**Endpoints requiring manual follow-up:**
- None — all 5 endpoints were auto-testable; no endpoints were skipped.

---

*Report generated by api-probe skill. Intermediate files: `.agent-tasks/batch2_routes.json`, `.agent-tasks/batch2_results.json`, `.agent-tasks/env_snapshot.json`.*
